from setuptools import setup, find_packages

setup(
    name="vendor-invoice-validator",  # Replace with your package name
    version="0.1",  # Replace with the actual version
    packages=find_packages(),
    install_requires=[
        # List your dependencies here
    ],
)
